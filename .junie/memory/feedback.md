[2026-04-07 21:13] - Updated by Junie
{
    "TYPE": "correction",
    "CATEGORY": "Project structure",
    "EXPECTATION": "Keep a single Cargo package at the root and put each concept as its own numbered file in src/bin (e.g., 01_helloworld.rs, 02_comments.rs), with exactly one concept per file, runnable via cargo run --bin <name>.",
    "NEW INSTRUCTION": "WHEN organizing Rust exercises THEN place each concept as numbered file in src/bin"
}

